fastapi==0.95.2
uvicorn[standard]==0.22.0
jinja2==3.1.2
python-dotenv==1.0.0
sqlalchemy==2.0.19
pydantic==1.10.11
sentence-transformers==2.2.2
torch==2.3.0 # UPGRADED: Updated to a newer, stable version to avoid implicit torchaudio conflict.
torchaudio==2.3.0 # NEW: Explicitly set a torchaudio version compatible with the new torch version.
torchvision==0.18.0 # NEW: Added torchvision, often used alongside torch/torchaudio, to ensure compatibility.